<%= form_with(model: historico_atividade, local: true) do |form| %>
  <% if historico_atividade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(historico_atividade.errors.count, "error") %> prohibited this historico_atividade from being saved:</h2>

      <ul>
      <% historico_atividade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :historico_atividade_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
