<p id="notice"><%= notice %></p>

<h1>Atividades</h1>

<table>
  <thead>
    <tr>
      <th>Titulo</th>
      <th>Datainicio</th>
      <th>Datafim</th>
      <th>Cargaprev</th>
      <th>Cargareal</th>
      <th>Descricao</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @atividades.each do |atividade| %>
      <tr>
        <td><%= atividade.titulo %></td>
        <td><%= atividade.dataInicio %></td>
        <td><%= atividade.dataFim %></td>
        <td><%= atividade.cargaPrev %></td>
        <td><%= atividade.cargaReal %></td>
        <td><%= atividade.descricao %></td>
        <td><%= link_to 'Show', atividade %></td>
        <td><%= link_to 'Edit', edit_atividade_path(atividade) %></td>
        <td><%= link_to 'Destroy', atividade, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Atividade', new_atividade_path %>
